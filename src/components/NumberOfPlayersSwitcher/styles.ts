import { BoxProps, IconButtonProps } from "@chakra-ui/react"

export const BoxStyle: Partial<BoxProps> = {
  display: 'flex'
}

export const IconButtonStyle: Partial<IconButtonProps> = {
  size: 'md',
  fontSize: 'lg',
  variant: 'ghost',
  color: 'current'
}